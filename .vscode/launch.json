{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Plugin prototype pocketbase hook calculated fields",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/backend/main.go",
      "args": ["serve"],
      "cwd": "${workspaceFolder}/backend/"
    }
  ]
}